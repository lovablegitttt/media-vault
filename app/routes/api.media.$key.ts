import type { Route } from "./+types/api.media.$key";
export async function loader({ request, context }: Route.LoaderArgs) { const pathname = new URL(request.url).pathname; const key = decodeURIComponent(pathname.replace(/^\/api\/media\//, "")); if (!key || key.includes("/")) return new Response("Not found", { status: 404 }); const object = await context.cloudflare.env.UPLOADS.get(key); if (!object) return new Response("Not found", { status: 404 }); return new Response(object.body, { headers: { "Content-Type": object.httpMetadata?.contentType ?? "application/octet-stream", "Cache-Control": "private, max-age=3600" } }); }
